"""schema v4 配置存储的服务层导出。"""

from core.config import (  # noqa: F401
    CONFIG_DIR,
    CONFIG_FILE,
    CONFIG_VERSION,
    DATA_DIR,
    PROJECT_ROOT,
    SRC_DIR,
    get_config_section,
    invalidate_config_cache,
    load_config,
    save_config,
    set_config_value,
)
